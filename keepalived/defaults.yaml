# -*- coding: utf-8 -*-
# vim: ft=yaml
---
keepalived:
  package: keepalived
  config_file: /etc/keepalived/keepalived.conf
  service:
    name: keepalived
  config:
    global_defs:
      smtp_server: localhost
  scripts_dir: /etc/keepalived
  scripts: {}
